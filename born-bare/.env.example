# Supabase (public — safe to ship to the browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx

# Supabase service role (server only — never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# Site origin (used for absolute URLs in referral links and metadata)
NEXT_PUBLIC_SITE_URL=https://wearebornbare.com
